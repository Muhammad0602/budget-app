<p style="color: green"><%= notice %></p>

<nav class='navbar'>
  <a href="<%= root_path %>" class="back-btn">&larr;</a>
  <h3>Categories</h3>
  <%= link_to 'Log Out', destroy_user_session_path, 'data-turbo-method': :delete, class: 'next-btn' %>
</nav>

<div id="categories">
  <% @categories.each do |category| %>
    <%= link_to category do %>
    <%= render category %>
    <% end %>
  <% end %>
</div>

<div class="new-category">
  <%= link_to "NEW CATEGORY", new_category_path, class: 'category-btn' %>
</div>

