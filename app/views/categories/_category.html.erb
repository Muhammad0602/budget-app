<div class="category-container" id="<%= dom_id category %>">
 
<%= image_tag(image_path(category.icon), class: "icon-image") %>

  <p>
    <%= category.name %>
  </p>

  <p class="money">$400</p>

</div>
